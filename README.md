# ETERNAL CRAFT SMP

Paper Minecraft server configuration managed by Dawn.

The live `server.properties`, world data, libraries, server JARs, logs, and local secrets are intentionally excluded from Git.

To restore the basic server configuration, copy `server.properties.example` to `server.properties` and set local values.
